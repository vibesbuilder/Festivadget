# Geländeplan

Hier gehört der echte Geländeplan als `gelaendeplan.webp` (1024×768, siehe `content/map.json`).

Bis dahin zeigt die Karten-Ansicht (Phase 3) einen Platzhalter. Die POI-Koordinaten in
`content/pois.json` beziehen sich auf das Pixelraster dieses Bildes (CRS.Simple, §12.4).
